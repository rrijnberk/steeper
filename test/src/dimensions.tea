type dimensions {
    element input {
        attribute border-width: 1px : [ The width of input borders (with some stuff). ];
        modifier disabled {
            attribute border-width : 2px : [The width of disabled input borders];
        }
    }
}
